namespace Shiny.Maui.Controls;

/// <summary>Which edge of the page a <see cref="ProgressLine"/> runs along.</summary>
public enum ProgressLinePosition
{
    /// <summary>Across the top, under the navigation bar when there is one.</summary>
    Top,

    /// <summary>Across the bottom, above the tab bar when there is one.</summary>
    Bottom
}
