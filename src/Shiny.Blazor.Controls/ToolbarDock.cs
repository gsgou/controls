namespace Shiny.Blazor.Controls;

/// <summary>
/// Determines which edge a <see cref="ShinyToolbar"/> or <see cref="ShinyTabBar"/> docks to.
/// </summary>
public enum ToolbarDock
{
    /// <summary>Docks to the top of the scroll container / screen.</summary>
    Top,

    /// <summary>Docks to the bottom of the scroll container / screen.</summary>
    Bottom
}
