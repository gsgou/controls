@namespace Shiny.Blazor.Controls.Splash
@inject ISplashScreen SplashScreen
@* Renders nothing. Its only job is to hand off from the pre-boot splash to the app at the
   right moment - first render by default, or whenever ReadyWhen finally returns true. *@
