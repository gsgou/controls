@namespace Shiny.Blazor.Controls

@*
    A full-height rule between two runs of items, and a break in the column flow: it spans every row of
    the group's grid, so the items after it start a fresh column rather than filling up the one before.
*@
<div class="shiny-ribbon-sep" role="separator" aria-orientation="vertical"></div>
