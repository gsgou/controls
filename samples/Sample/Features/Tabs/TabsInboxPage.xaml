<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:shiny="http://shiny.net/maui/controls"
             xmlns:local="clr-namespace:Sample.Features.Tabs"
             x:Class="Sample.Features.Tabs.TabsInboxPage"
             x:DataType="local:TabsInboxViewModel"
             Title="Inbox"
             shiny:ShinyTabs.Badge="{Binding UnreadText}">

    <!-- The centre button's menu, declared by the page the way ToolbarItems are. -->
    <shiny:ShinyTabs.Actions>
        <shiny:TabActionCollection>
            <shiny:TabAction Text="New message" Icon="edit" Command="{Binding ComposeCommand}" />
            <shiny:TabAction Text="Mark all read" Icon="check" Command="{Binding MarkReadCommand}" />
            <shiny:TabAction IsSeparator="True" />
            <shiny:TabAction Text="Empty inbox" Icon="trash" IsDestructive="True" Command="{Binding EmptyCommand}" />
        </shiny:TabActionCollection>
    </shiny:ShinyTabs.Actions>

    <ScrollView>
        <VerticalStackLayout Padding="24" Spacing="12">
            <Label Text="A ContentPage in a tab" FontSize="20" FontAttributes="Bold" />
            <Label FontSize="14" TextColor="Gray"
                   Text="This tab's ContentTemplate inflates a whole ContentPage. Its Title fills in the tab label, its BindingContext reaches the hosted content, and the ShinyTabs values above are read straight off it." />

            <BoxView HeightRequest="1" Color="LightGray" />

            <Label Text="{Binding Log}" FontSize="14" />
            <Label FontSize="13" TextColor="Gray" Margin="0,4,0,80"
                   Text="Tap the centre button to see this page's actions. Switch tabs and come back — OnTabAppearing runs each time." />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
